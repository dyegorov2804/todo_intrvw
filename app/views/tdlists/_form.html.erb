<%= form_with(model: tdlist, local: true) do |form| %>
  <% if tdlist.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(tdlist.errors.count, "error") %> prohibited this tdlist from being saved:</h2>

      <ul>
        <% tdlist.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_field :description %>
  </div>

  <div class="field">
    <%= form.label :completed %>
    <%= form.check_box :completed %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
